val t: Traversable[Int] = Traversable(1, 2, 3)

t.head

t.tail

t.isEmpty

t.sum

t.init

t.size

val u: Traversable[String] = Traversable("Hello ", "World")

val k = Traversable(1, 2, 3)

val i: Traversable[Any] = Traversable(1, "a")

t.foreach(el => println(el))

val s = k
